# Pinned upstream0.1.10 core-only manifest; browser wrappers are explicitly unavailable.
# Upstream normalized manifest and per-file hashes retained adjacent to this file.
[package]
name = "proj4rs"
version = "0.1.10"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/3liz/proj4rs/"
publish = false
[lib]
crate-type = ["rlib"]
[features]
default = []
esri = []
wasm-strict = []
multi-thread = []
geo-types = []
logging = []
aeqd = []
krovak = []
local_tests = []
proj4js-compat = []
with-wasm-entrypoint = []
crs-definitions = []
[dependencies]
thiserror = "=2.0.20"
libm = "=0.2.16"
