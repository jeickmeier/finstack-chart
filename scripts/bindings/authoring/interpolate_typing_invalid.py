import finstack_chart as c
c.interpolate_number(0, 1).sample("0.5")
c.interpolate_number.gamma(2)
c.numeric_array("BigInt64Array", [1, 2])
c.quantize(c.interpolate_number(0, 1), 2.5)
c.interpolate_zoom.rho("1")
c.piecewise(lambda a, b: a, [0, 1])
