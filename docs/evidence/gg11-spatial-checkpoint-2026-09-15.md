# GG11 spatial implementation checkpoint

GG2-05/06 and FIX-GG11. Prerequisites GG06/07/09 are accepted by the coordinating owner. This slice owns rectangular/hex summaries, 2D KDE, contour/isoband topology, ellipse kernels, generated schemas/stages, shared polygon/path recipes and independent host authors. Root owns common Stat visitors, coordinated host builds and the status ledger. Definition version 79 retains SpatialSpec and source hex recipes. This checkpoint does not close the package.

The independent source capture `tools/reference/r/spatial-statistics-controls.R` now generates 33 pinned ggplot2 4.0.3 cases in `fixtures/parity/ggplot2/spatial-statistics-controls.json`, exercising both build and draw. Cases cover rectangular membership/closure/empty cells, response summaries, hex membership, missing responses and weights, KDE bandwidth/adjustment/ignored weights, saddle cells, rings/holes, missing cells, rotated grids, weighted normal/t/Euclidean ellipses and degeneracy. `spatial-contour-levels.R` captures 28 threshold-selection cases and three endpoint-label precision cases. Exact ggplot2 4.0.3, hexbin 1.28.6 and isoband 0.3.0 versions fail fast. An initial absent hexbin dependency was caught from empty outputs and diagnostics; it was installed only into the temporary development oracle library, then all source captures regenerated. No runtime dependency was added.

Source method captures are `/private/tmp/gg11-source-contract.txt`, `/private/tmp/gg11-helper-contract.txt`, `/private/tmp/gg11-hex-grid-contract.txt`, `/private/tmp/gg11-contour-grid-contract.txt`. The pinned isoband source was inspected for saddle classification, masking and threshold policy; the implementation uses a shared edge graph and band-boundary composition, not a second renderer. Fixture regeneration log: `/private/tmp/gg11-spatial-fixture3.log`.

`grammar/spatial_types.rs` owns the portable controls. `grammar/spatial/` owns bounded kernels: shared GG06 fuzzy rectangular bins, triangular hex lattice membership, Gaussian product KDE using shared bandwidth selection, robust/weighted covariance ellipses, marching-square line graph, band rings, panel-wide thresholds and rotated-grid normalization. `spatial_stage.rs` retains exact aggregate source membership; derived grid/path rows share one immutable member allocation per group. Grid cells, segments, generated rows, catalogs and retained aesthetics have explicit existing limits. Missing coordinates/responses and invalid ellipse groups follow the common invalid policy. KDE ignores weights as the source does; cell counts and ellipses retain their own weight contracts.

Generated categorical levels feed the existing color-scale owner, preserving band order and explicit named palette overrides. Hex geometry uses the shared polygon path, fill-rule and inspection consumers. Sixteen independent Rust/Python/WASM authors cover 48 publications per host; all compare original and replay scenes before export. The native gallery covers representative count, hex, density, filled topology and ellipse cases.

Final focused qualification:

- `mise exec -- cargo test -p chart-core --lib grammar::spatial --locked`: **9 passed**, `/private/tmp/gg11-final-kernels6.log`, including all source kernels, rotated contour/band topology, threshold/label precision and shared membership.
- `mise exec -- cargo test -p chart-core --test ggplot_spatial_controls --locked`: **5 passed**, `/private/tmp/gg11-final-authors8.log`. These exercise all sixteen authors/replay, actual ordered band colors/catalogs/auxiliary fields, empty density, explicit palette override, source physical line width in both destination units and 300-sample continuous colorbars.
- Scoped Clippy `/private/tmp/gg11-scoped-clippy2.log` had no GG11 findings; independent model/coordinate findings were reported to their owners. This is not an aggregate check pass.
- Rust author/export: `/private/tmp/gg11-spatial-rust-final2.log`, **48 SVG/PDF/PNG publications** and original/replay scene identity. All sixteen triplets inspected in `/private/tmp/gg11-spatial-rust-final2-inspection/review-1.png` through `review-6.png`: **PASS**. Corrected continuous colorbars, colored band rectangle keys, source line widths, holes/missing cells/saddles, KDE tiles and all ellipse kinds were observed. Tile boundary antialiasing differs between SVG/PNG and Poppler PDF rasterization; the underlying shared cells remain identical.
- Native gallery built with `cargo build -p chart-gallery --example ggplot_spatial_controls_native --locked`. Final supplied-font gallery inspected **PASS** at `/var/folders/l1/s1m3_kfn43d77mc45c_3rv_h0000gn/T/codex-shot-2026-09-15_23-36-14.png`. `/private/tmp/gg11-spatial-native.log` records all six presented stamps at layout 1 and three paints. Only the owned process was closed (session 58270, exit 130); focus released.

Inspection caught two real presentation gaps before acceptance: spatial ShapeLine widths bypassed the reference unit conversion, and automatic fill guides used black point glyphs. The narrow fixes classify only Spatial ShapeLine widths, prevent duplicate Hexagon outline conversion, choose existing continuous colorbars for automatic generated fills and rectangle keys for Spatial surface recipes. Existing nonspatial guide/default behavior is unchanged. Initial `/private/tmp/gg11-spatial-rust` and `-final` exports are superseded by `-final2`.

Runtime is frozen. Actual Python/WASM execution and byte equality, strict consumer qualification, aggregate repository checks/tests and final package acceptance remain with the coordinating owner. The independent sixteen-author Python/WASM scripts, portable factories and declarations are ready; no host pass is inferred from Rust or native evidence. The status ledger remains IN PROGRESS pending those gates.
