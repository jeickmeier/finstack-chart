# GG-18 integration checkpoint

Partial, revision `295e7a0ec2e9145b140c838751b615ee40e9f64a` plus shared working tree, macOS arm64. GG-18 acceptance remains dependent on GG-14–17 and cumulative validation.

Three independent Rust/Python/Node WASM authors combine weighted-capable model machinery (linear fit here), generated confidence ribbons, source observations, grouped facets, registered facet planning, custom legend drawing and key glyphs, paired nonlinear coordinates, a reference theme, global tag and shared mathematical text. Cases separate a source-filtered fit from unchanged-fit coordinate zoom. Large source keys start above2^53 and remain exact in each language.

Three focused Rust integration tests pass (`/private/tmp/gg14-18-isolated-tests.log`): source filtering changes model population12→8 per group while coordinate/state zoom leaves model rows unchanged; append/upsert/remove/retention recompute exact values/counts/members and displayed primitives equal to fresh batch authors; rejected duplicate append preserves committed results; captured old SVG remains identical after updates/disposal. The full Presented/Current × Visible/FullDomain × interaction inclusion matrix pins the correct source/definition/view policies through disposal, retains selected source targets and exposes derived model provenance rather than fabricated source rows.

Actual fresh Python and Node WASM scripts pass independent authors, update-versus-batch primitive equality, rejected update, eight captured combinations, explicit callback registry retention and disposal (`/private/tmp/gg18-python-first2.log`, `/private/tmp/gg18-wasm-first2.log`). These tests use modules from `/private/tmp/finstack-chart-proof-20260916/gg14-16-18-final`; their Rust/Python/WASM publication byte comparison is still running in the integrated runner.

Nine initial Rust SVG/PDF/PNG outputs were independently rendered with the existing SVG verifier and Poppler and all inspected (`/private/tmp/gg18-first/inspection/all-publications.png`). Nonlinear ribbons/grids, reversed facets, math fractions/radicals, custom guide and source-filter/zoom distinctions agree across destinations. Native integration gallery is implemented and queued for inspection. Geographic/custom-model combinations, completed GG17 device matrix and final cumulative schema/stub/legacy/single-thread/repository qualification remain open.

For build throughput, a task-specific cache at `/private/tmp/finstack-chart-gg-final-target` uses `CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0`. The first focused core/export build took42seconds and1.6GB; this avoids contention with the editor's automatic all-target workspace checks without modifying editor configuration or stopping user processes. These settings affect debug symbols, not numerical optimization or feature selection.

## Expanded actual-host capture matrix

The Python/CJS scripts now select both a source row and an inspected derived model path, hover the derived path, and acknowledge presentation at viewport[4,8]. They then append data, apply a pending title, and set viewport[5,9] before retaining the eight capture combinations. Assertions compare actual captured state, source and definition revisions, effective selection/hover, effective viewport and exported title after all owning objects are disposed. Python's isolated actual-module run passes `/private/tmp/gg18-matrix-python.log`: Presented remains store3/definition0 while Current captures store4/definition1; interaction inclusion yields exactly two selected targets and one hover; full-domain clears the viewport. Records are `/private/tmp/gg18-matrix-python/capture-matrix.json`. The corresponding WASM run passes `/private/tmp/gg18-matrix-wasm.log`; its eight records are exactly equal to Python after ordinary JSON decoding. Independent original/replay scene comparisons are also included in all four publication authors, including the new registered-model/geographic mode3.

## Consolidated integration qualification

Five focused Rust tests pass in `/private/tmp/gg14-18-final-focused.log`. Additional checks select and resolve both exact aggregate membership and derived model scope from presented geometry; replay both LibraryV1 and the reference profile while retaining complete scale/guide definitions; and combine geographic projection, prescribed-slope custom models, facets, theme and math. This exposed and corrected a real LibraryV1 confidence-band bug: a deliberately absent zero-width outline was emitted as an invalid stroke after independent-paint conversion. The shared conversion now omits that stroke while retaining the filled path.

Fresh actual Rust/Python/Node WASM proofs pass at `/private/tmp/finstack-chart-proof-20260916/gg15-16-18-final2`, with12identical publication bytes per host and identical scene JSONs. Python and WASM independently select source and derived marks, hover derived results, capture a presented viewport, then change source, definition title and viewport; all eight capture combinations verify actual retained/effective state and revisions after disposing their owners. Four integrated SVG/PDF/PNG sets were independently rendered and inspected in `ggplot_integration_controls/rust/inspection/all-publications.png`. Native four-mode paint inspection passed; recorded pointer interaction is the final native boundary. Cumulative legacy/primary proofs and workspace checks are running separately; these focused successes do not claim those gates.

Native programmatic interaction qualification also passes. The opt-in `GG18_PROGRAMMATIC_PROBE=1` in the existing integration gallery uses only public `ChartView::dispatch_inspection`, `dispatch_chart` and `capture_presented`. It resolves a source target from painted geometry, hovers/selects it, changes the viewport, then resolves and hovers a derived model target from the new painted geometry. The captured selection/hover exactly equal current native state, and the captured viewport revision equals the reducer's viewport revision. `/private/tmp/gg18-programmatic-native2.log` records `integration-programmatic-PASS`: definition/store0, layout2, presentation state3, viewport1, inspection-inclusive state5; source key9007199254740994 and a derived model target are retained. All paint diagnostics are null. The selected marker and changed x window[-3,3] are visibly inspected in `/var/folders/l1/s1m3_kfn43d77mc45c_3rv_h0000gn/T/codex-shot-2026-09-16_21-07-11-w10279.png`. Scoped gallery Clippy passes (`/private/tmp/gg18-programmatic-clippy2.log`), and the modified example is formatted. Both owned proof processes were closed. This establishes public native programmatic action/capture coherence; physical pointer automation remains unverified because CUA could not target the executable and its temporary app attempt was interrupted. No claim of physical pointer qualification is made.
