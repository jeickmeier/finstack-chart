# GG07 portable stroke controls — 2026-09-15

`Style.line_end` (butt/round/square) and `Style.line_join` (miter/round/bevel) are optional retained controls. Existing scenes without either option retain their prior primitives. Explicit controls lower once in core layout to one nonzero filled compound path per stroke, consumed unchanged by native, SVG, PDF, PNG and inspection. All segment rectangles, caps and joins have the same winding: overlapping contours form one paint operation, preserving translucent alpha. Original source anchors/targets remain attached. Existing fills remain separate from the generated stroke outline.

Miter joins use the pinned grid default limit 10 and fall back to bevel beyond it. Round components use deterministic libm cubic construction with adaptive subdivision, conservative radial error below 0.01 destination units, and checked generated command budgets. Dash splitting happens before cap expansion, so every dash receives the selected cap. Width conversion precedes this shared owner; explicit point units work in logical-pixel and point destinations. No renderer-specific cap/join flags or new dependencies were introduced.

Independent reference capture: `tools/reference/r/stroke-controls.R` → `fixtures/parity/ggplot2/stroke-controls.json`, using pinned ggplot2 4.0.3 / R 4.6.1. Nine lineend/linejoin combinations retain draw grob linewidth, alpha, linetype and miter limit. Related interval component controls come from `interval-paint-controls.R` and its fixture.

Focused evidence:

- `mise exec -- cargo test -p chart-core --lib portable_ --locked`: 5 PASS, `/private/tmp/gg07-stroke-final.log`. Cap boundaries, clockwise/counterclockwise joins, overlap winding, miter fallback, individual dashed-run caps/gaps, unchanged optional default, generated budget failure, and radius 0.001 through 1e9 error bounds.
- Core paint/default/source tests: 5 PASS and surface tests: 8 PASS, `/private/tmp/gg07-paint-surface-final2.log`. Includes authored point units in both destinations, recipe-kind-driven Count defaults and missing mapped tile Width/Height omission. Cap inspection after Plot wire replay is additionally checked in `/private/tmp/gg07-cap-hit-final.log`.
- `mise exec -- cargo test -p chart-export --test ggplot_stroke_controls --locked`: 1 PASS, `/private/tmp/gg07-stroke-export-final.log`. Actual PNG pixels at cap/body overlap, two-segment overlap and bend center all retain one alpha blend, exactly `[255,127,127]`; actual SVG uses nonzero fill and PDF exports successfully.

Independent Rust/Python/WASM authors and native gallery are owned by the integration coordinator (`ggplot_stroke_controls`), six cases including dashed caps and translucent solid joins. Aggregate host publication equality and final native inspection remain integration evidence until the shared runner completes; the focused checks above do not claim those gates.

Rectangle outlines use the existing `independent_paints` conversion to a closed ShapePath before optional stroke expansion; no extra rectangle renderer path is necessary. The focused closed-rectangle sentinel checks join corners, no painted center and identical output regardless of open-cap setting (`/private/tmp/gg07-stroke-rectangle.log`). Cap containment hit/miss tests after Plot replay pass; automatic line inspection intentionally retains its prior nearest-x fallback policy.

Final native inspection passed all six stroke modes, screenshot `/var/folders/l1/s1m3_kfn43d77mc45c_3rv_h0000gn/T/codex-shot-2026-09-15_20-54-02.png`, trace `/private/tmp/gg07-native-strokes-final.log` with six valid scene stamps and 14 painted frames. All six SVG/PDF/PNG triplets in the final Rust proof were opened and inspected in `ggplot_stroke_controls/rust-inspection/review-{1,2}.png`. Uniform alpha and distinct cap/join shapes are visible. The integration owner is refreshing dashed host equality after making the common dash splitter hypot deterministic; final acceptance is recorded centrally.

Final cross-host qualification passed: `/private/tmp/gg07-final-hosts2.log`, artifacts `/private/tmp/finstack-chart-proof-20260915/gg07-final2`. All 111 GG07 publications, including all 18 stroke publications, are byte-identical across independent Rust/Python/WASM authors. `inspection-reuse.json` confirms every final2 Rust publication is byte-identical to the already inspected final Rust output. The common deterministic dash correction therefore preserves the completed visual inspection; no additional image review is necessary.
