# GG-17 explicit save policy checkpoint

Revision `295e7a0ec2e9145b140c838751b615ee40e9f64a` plus working tree. This is partial GG-17 evidence; device qualification, multipage host routes and full repository checks remain open.

`SaveOptions` resolves inches/centimeters/millimeters/pixels, scale, numeric or named DPI, optional dimensions from an explicitly supplied current page, the reference fifty-inch limit, filename inference and explicit device overrides. Filename placeholders support `%d`, `%0Nd` and `%%`. No ambient R/current-device state is emulated. Numeric DPI retains the existing positive integral publication-density contract; named screen/print/retina are72/300/320. Invalid dimensions, missing explicit page defaults and malformed placeholders reject before writes. `SavePlan::options` preserves caller background/text/capture settings while applying dimensions and density.

`DeviceRegistry` holds at most64 uniquely named explicit callbacks, pins cloned registrations and cannot replace built-in names. A callback consumes the same immutable `FigureSnapshot` as built-in export. Errors retain their original diagnostic; returned bytes are checked against the explicit budget. Encoding performs no filesystem operations. `SavePlan::write` creates parents only when explicitly selected and otherwise preserves the original I/O error.

Python `Output.resolve_save` and `save_figure` execute shared resolution; a host callback executes synchronously over a retained figure, and bytes are copied and validated before the requested write. WASM exposes the same policy and callback route, returning owned `{plan, bytes}` for a host download/write. It does not assume Node filesystem access in browser code. Both routes dispose temporary requests/figures/options on success or exception. Existing explicit low-level export/save APIs remain available.

The initial source fixture accidentally called the private R `plot_dim` helper with width/height arguments instead of its `dim` vector, yielding26 irrelevant errors. The generator was corrected after inspecting pinned R4.6.1/ggplot2 4.0.3 source; committed dimensions now contain24 numeric successes and2 expected size-limit errors. All26 are checked against Rust and actual Python/Node WASM. Existing22 actual TIFF codec cases are retained. Source inspection log: `/private/tmp/gg17-source.log`; corrected generator log: `/private/tmp/gg17-devices-source-fixed.log`.

Two Rust tests pass (`/private/tmp/gg17-save-tests2.log`): all reference dimensions, filenames, explicit fallback page, custom errors/duplicate registration/pinning/byte budget, and opt-in directory creation. Actual fresh modules in `/private/tmp/finstack-chart-proof-20260916/gg14-16-17-first` pass Python and WASM save scripts (`/private/tmp/gg17-save-python2.log`, `/private/tmp/gg17-save-wasm2.log`), including PNG bytes, custom-device identity, exception identity, final-byte budget and no failed-write artifact. Strict Python and TypeScript consumers passed in the shared build. The Python path creates and cleans a private temporary directory; the WASM path returns owned bytes.

Retained FigurePages now shares PDF/PostScript/TIFF encoders, accepts1–1024immutable pages, preserves per-page fonts/profile/dimensions, rejects multi-page EPS and unsupported page formats, and applies cumulative byte limits. Rust single-page PDF remains byte-identical to ordinary export. Fresh independent Rust/Python/Node WASM authors export two-page PDF/PS/TIFF after original figure/request disposal; all30device files per host match (`/private/tmp/gg17-final.log`). Poppler reports two correctly sized PDF pages, Ghostscript decodes two PS pages, and libtiff independently decodes both TIFF directories. Save-policy scripts pass all26source dimensions, custom callback errors/budgets and actual explicit host I/O boundaries. Windows EMF playback remains a separate unexecuted platform gate.
