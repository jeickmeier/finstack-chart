# GG-16 implementation checkpoint, 16 September 2026

Status: partial; GG-16 remains open. Revision 295e7a0ec2e9145b140c838751b615ee40e9f64a plus shared working tree. This checkpoint predates full guide drawing and standalone summary host qualification.

The primary Rust/Python/WASM APIs now expose checked cut/resolution vectors, owned category materialization with retained unused levels, registered legend keys, explicit data materialization and typed autoplot/autolayer dispatch through the existing engine. Explicit returned level domains preserve ordered guides without changing legacy character-scale semantics. ADR-032 records registration and ownership contracts.

Actual fresh native Python and wasm32/Node builds passed the scoped GG-16 runner, including five authors and 15 SVG/PDF/PNG publications per host, 35 independent ggplot2 4.0.3 vector cases, copy/disposal, registered round-trip and matching Rust/Python/WASM output. Output: `/private/tmp/finstack-chart-proof-20260916/gg16-second`; command log: `/private/tmp/gg16-second-hosts.log`. The built modules include the corrected native/WASM proj4rs parser. The contact sheet `ggplot_extension_controls/inspection/all-publications.png` was inspected: all 15 SVG/PDF/PNG renderings agree in ordering, geometry and custom diamond keys.

Focused key and authoring tests passed in `/private/tmp/gg16-protocol-tests.log`. Three vector tests, including independent mean/standard-error expectations and missing/infinite-value handling through the shared stat owner, passed in `/private/tmp/gg16-summary.log`. New strict declaration consumers have been added but not rerun after adding standalone summary and guide drawing. The earlier declaration pass did not exercise these newest APIs.

Remaining GG-16 work: complete coordinate/facet extension protocols and external examples, qualify full guide drawing and standalone summary in actual hosts, native publication, malformed-output/updates/snapshot lifetimes at all extension boundaries, then repository checks and acceptance evidence. This checkpoint does not close GG-16, GG-18, global parity, Linux or performance gates. Next action: finish bounded extension protocols and batch their actual-host/native qualification.

A second expanded checkpoint passed nine external protocol tests (keys, full guides, facets, coordinates and materialization/recipes) in `/private/tmp/gg16-all-new-protocol-tests.log`. Fresh Rust/Python/Node WASM then passed eight authors and 24 publications per host, strict new API consumers, 35 vector controls, standalone summary checks, native-only protocol rejection and registry copy/disposal retention: `/private/tmp/gg16-third-hosts.log`, output `/private/tmp/finstack-chart-proof-20260916/gg16-third`. All 24 SVG/PDF/PNG renderings were inspected. That inspection found custom-wave curves had an insufficient enclosure contract despite cross-host agreement. The third checkpoint therefore does **not** qualify coordinate path fidelity. The repair adds required conservative normalized bounds to `TrainedCoordinate`, checked by the shared subdivision owner, with an independent hidden-extrema regression. A fresh affected host/native proof is required after that repair.

The enclosure repair now passes three focused coordinate tests, including a two-endpoint line with hidden full-wave extrema (`/private/tmp/gg16-coordinate-enclosure2.log`). All five protocols compose in a real supplied-font live test: append/upsert/remove/count retention equal a fresh batch, duplicate append rejects without changing output, and an old prepared snapshot retains its two panels and ink (`/private/tmp/gg14-16-root-tests.log`). The test moved to chart-export because the minimal external test measurer deliberately cannot shape rich facet labels.

Fresh native eight-panel rendering has nonnull stamps, null diagnostics and was inspected (`/private/tmp/gg16-native-final.{png,log}`). The short colored wave segments are correctly subdivided (1025/1209/2049 commands in the fresh Rust scene); orange/green arc deviations are only about 0.9/1.9 points, so apparent straightness is not a projection failure. A full-width independent horizontal line was added to the eighth author in all three languages to make the hidden-extrema proof visually conspicuous. Fresh hosts and native recapture of this updated author remain pending.

The updated full-wave author now passes fresh Python/WASM and Rust publication comparisons in `/private/tmp/finstack-chart-proof-20260916/gg14-16-17-first/ggplot_extension_controls` (`/private/tmp/gg16-17-first2-hosts.log`, before the separate device mismatch). All24 independent SVG/PDF/PNG renderings were inspected; the full wave is visible and agrees across formats. Save/device body changes after module capture explain the subsequent GG17 PS/EPS comparison failure; they do not invalidate the completed GG16 comparison. A final requirements audit identified two explicit remaining boundaries: registered facet labellers (current facet labeller was recipe-only) and external CustomModel runtime qualification (trait existed, external fixture was absent). Both are now assigned bounded implementation/qualification work; GG16 stays open.

Prescribed-slope model boundary: `examples/custom-extension/src/models.rs` supplies a portable and native-only CustomModel with schema-checked slope/envelope parameters. This deliberately simple external example computes a weighted intercept and consumes the explicit prediction grid and confidence options; its envelope is an application policy, not a statistical confidence-estimator claim. Three independent tests pass (`/private/tmp/gg16-custom-model-tests2.log`), covering exact closed-form values, weights, repeated preparation, retained registry, wire replay, missing registry/native-only rejection, malformed length/nonfinite/negative-SE/inverted-band output. Independent Rust/Python/WASM author mode8 now uses the serialized revision string and full ModelOptions contract. Root's final ten-author30-publication/host comparison includes this mode and passes byte/scene equality.
