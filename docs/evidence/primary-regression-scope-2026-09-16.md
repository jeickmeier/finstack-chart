# Final primary-authoring regression scope

The optional broad `scripts/run_primary_authoring_proofs.py` rerun was deliberately
stopped by the integration owner after71 completed commands, during command72
(`position-functions-wasm`, script line195). Exit130/KeyboardInterrupt is the
intentional interruption, not a passing whole-run result or a discovered failure.
No fixture or assertion was weakened. Completed artifacts remain in
`/private/tmp/finstack-chart-proof-20260916/gg06-18-cumulative2`; complete execution
log is `/private/tmp/gg06-18-cumulative2.log`.

Fresh Rust/Python/Node WASM primary authors, all34 primary semantic/scene families,
23 action transitions plus47 input steps,70-step streaming replay, exact IDs/data,
copy/accessor ownership, edits/capture matrix, stale fences, disposal/retention and
strict TypeScript/mypy consumers passed. GG02's13 independent stage authors passed
across all three hosts. The completed additional legacy GG04 checks include time
formats,2304 authored-limit/fractional-bin configurations (including the repaired
constant-limit duplicate-break regression), positional bins, numeric/temporal/
discrete minor selection,3558 limit/order configurations, nullable categories,
3840 nullable positions,520 positional palettes, temporal guides,198 registered
discrete-limit states,577 identity-callback states plus18 byte-identical publications,
and1348 numeric-limit states per host. The subsequent five Rust callback tests and
1584 Python positional-callback states passed; their WASM counterpart was interrupted
and is **not claimed complete**.

The remaining broad runner mostly repeats accepted GG04 matrices and D3 scale,
chromatic, axis, hierarchy and GG03 evidence. GG18's plan requires actual primary
package/declared-surface/legacy-interchange behavior and says to reuse existing export
coverage. It does not require repeating this entire optional runner. The required
`bindings-proof`, fresh assigned-family host proofs and aggregate workspace/check
results are separately owned and recorded by integration. This note does not close
GG19, G-GGPLOT, G-PARITY or production/platform certification, and does not claim a
complete cumulative primary-authoring rerun.
