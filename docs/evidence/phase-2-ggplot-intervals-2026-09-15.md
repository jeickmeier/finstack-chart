# GG07 interval and shared recipe implementation evidence

Scope: shared typed recipe inputs, preparation and projection; interval stems, point ranges, error bars, crossbars, reference equations, step directions and physical arrows. Root owns GG07 acceptance and integrated host/native evidence.

Pinned source: `tools/reference/r/primitive-recipes.R`, run through the repository R runner with local R 4.6.1/ggplot2 4.0.3, produces `fixtures/parity/ggplot2/primitive-recipes.json` (45 build/draw cases). Generation log: `/private/tmp/gg07-primitive-reference.log`. This includes successful and rejected device outcomes, not only computed data.

Shared contract: `Layer.recipe` and `recipe_aes` reuse numeric/category/statistical input readers. Bounds project into the dependent scale before positions; widths establish collision extents before positions; component expansion follows positioning and keeps original provenance. Deferred surface/mark/reference payloads share projection and budget checks. Category offsets retain category identities. Rug axes train independently. Count defaults use canonical StatSum and common numeric Size scales. Retained Count mappings are proven partition values, with exact evaluated numeric expressions retained separately from raw fields; no representative-row lookup.

Focused validation: `mise exec -- cargo test -p chart-core --test ggplot_recipe_intervals --locked` passed 5 tests (source intervals both orientations, source logarithmic bounds, horizontal literal logarithmic bounds, generated summary bounds plus blank scale training, nontraining reference lines and arrows). Final log `/private/tmp/gg07-interval-freeze.log`. Python compile and Node syntax checks passed for the two author scripts. These checks do not establish completed host/export/native acceptance.

Independent authors: `examples/common/ggplot_interval_recipes.rs`; export wrapper `crates/chart-export/examples/ggplot_interval_recipes.rs`; `scripts/bindings/ggplot_interval_recipes.py` and `.cjs`. Fifteen authors cover four interval families in both orientations, three steps, closed arrows at both ends and three reference equations. Each host emits 45 SVG/PDF/PNG artifacts named `interval-0` through `interval-14`, plus plot/scene JSON. Host scripts explicitly compare original and portable replay scenes. Root coordinates actual execution, comparisons and visual inspection.

Commands:

- `mise exec -- cargo run -p chart-export --example ggplot_interval_recipes --locked -- OUTPUT`
- `python3 scripts/bindings/ggplot_interval_recipes.py PYTHON_EXTENSION_DIR OUTPUT`
- `node scripts/bindings/ggplot_interval_recipes.cjs WASM_PACKAGE_DIR OUTPUT`

Native gallery is root-owned `ggplot_interval_recipes_native`; integration evidence remains in the central GG07 acceptance report. No full suite or host runtime build was performed by this subpackage owner.

Native inspection follow-up: Crossbar default fill corrected in shared projection to an unfilled outlined ShapePath, preserving explicit fill. Six scoped interval tests pass including both profiles default/explicit fill regression; `/private/tmp/gg07-crossbar-style.log`. Root owns regenerated host/native/export inspection.

Default-style audit follow-up: effective recipe kind now controls common point/line physical units, regardless of the builder's base Geom. Step/reference/rug use the reference linewidth conversion; Count retains point units, Pointrange's Point component uses source default size 0.5 times fatten 4 and stroke 1, and Crossbar's middle stroke uses default fatten 2.5. Explicit aesthetic units remain respected. Arrow norm/sin/cos use pinned libm, matching the shared deterministic math policy. Three independent source-style tests initially passed across ten line families in both device units plus point/fill defaults (`/private/tmp/gg07-paint-defaults-check.log`); final additional explicit-unit/base-geom and Crossbar middle-line regressions are owned by the surface agent. Actual regenerated host/native/export evidence remains root-owned.

Final independent style target (surface agent): first three tests pass including exact Crossbar middle linewidth2.5× and all earlier defaults. Fourth test confirms explicit point-unit overrides on Reference recipes are preserved across base builders; it exposed Count using its base Geom's paint route, assigned to mark owner for correction. Log `/private/tmp/gg07-paint-defaults-final.log`; this intermediate four-test run is not recorded as a pass. Shared interval/arrow runtime corrections are frozen pending that narrowly scoped mark-owner fix and final test.

Interval controls and edge completion: IntervalRecipe now has defaulted fatten, independent middle/box colour/linewidth/linetype and point size/shape/fill/stroke descriptors. Explicit middle linewidth replaces fatten-derived width. Point shapes reuse common Symbol/SymbolPaint and mapped Size/Shape channels. Crossbar middle/outline reset row alpha but preserve embedded colour alpha; box fill applies row alpha. Pinned `interval-paint-controls.json` now has ten cases including embedded RGBA. New `tools/reference/r/reference-line-controls.R` and fixture JSON capture eight actual build/draw cases: three constant-line constructors, duplicate mapped lines and four reversed-bound interval families. Constant line factories no longer inherit unrelated plot aesthetics; identical effective equations/styles/groups paint once with combined source targets. Reversed lower/upper endpoints remain drawable.

Final scoped interval target passes nine tests: `/private/tmp/gg07-interval-edge-final2.log`. Author scripts syntax-check. Independent Rust/Python/WASM author sets now have seventeen modes (new Crossbar component overrides and Pointrange point controls), producing 51 publications per host named `interval-0` through `interval-16`. Actual host/export/native execution and final acceptance remain root-owned.

Final missing-control/Count guard completion: common `number_or` defaults only absent channels; mapped Missing omits recipe ink. Common `retain_positions` retains finite primary/endpoints/bounds for omitted interval, surface and mark rows. Interval Width and reference Slope/Intercept use this policy. Count mapped Shape/Size validation now uses effective point semantics across Point/Rule/Rectangle base builders. Independent source fixture `fixtures/parity/ggplot2/missing-recipe-controls.json` records errorbar/tile/column/spoke/reference missing controls. Final combined scoped command `mise exec -- cargo test -p chart-core --test ggplot_recipe_intervals --test ggplot_recipe_marks --test ggplot_recipe_host --test ggplot_weight_host --locked` passed 22 tests (10 interval, 8 marks, 3 host, 1 weight). Log `/private/tmp/gg07-recipe-controls-host-final.log`. This is focused Rust/host-dispatch validation; actual Python/WASM/native/export and workspace gates remain root-owned. Runtime is frozen pending the root integration snapshot.
